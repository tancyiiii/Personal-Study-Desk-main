# Boundless Agents 比赛说明

## 赛道定位

个人终端 + 教育：一个运行在 Windows 桌面端的 AI 学习教练，面向“计划、执行、测评、复盘”的真实学习任务闭环，而不是泛聊天机器人。

## 目标用户与痛点

- 大学生和初入职场学习者：有学习目标，但计划不落地、进度难追踪、测评不及时。
- 考证和技能学习者：教材 PDF 多，不知道如何从材料中建立可执行的学习路径。
- 演示环境不确定：比赛现场可能没有稳定 API 或网络，因此内置离线示例模式。

## Agent 工作流

```text
学生信息
  │
  ├─ 学生分析 Agent：理解任务、识别能力差距
  ├─ 路线规划 Agent：编排阶段、里程碑、复习节点
  ├─ 资源检索 Agent：调用 Bing 中国区 / Baidu 搜索
  ├─ 测验生成 Agent：输出结构化 JSON，支持本机判分
  ├─ 辅导 Agent：保留多轮会话记忆
  └─ 文档问答 Agent：本地中文向量检索 + 来源引用
              │
              ▼
任务清单执行 → 自动判分 → 周度复盘 → 持续迭代
```

## 六项能力对照

| 评分能力 | 实现方式 |
| --- | --- |
| 任务理解 | 学生画像、知识水平、学习风格、时间约束和目标差距分析 |
| 流程编排 | 分析 → 路线 → 资源三步编排，业务层显式传递上下文 |
| 工具调用 | 国内搜索工具、本地向量检索、任务清单、结构化判分 |
| 知识增强 | ChromaDB + BGE 中文向量模型，PDF/文本分块、去重和来源引用 |
| 多轮交互 | 辅导 Agent 使用 PhiData 内存保留最近 8 轮会话 |
| 结果交付 | 路线、资源、测验 Markdown 下载；测验自动判分；任务进度持久化 |

## 5 分钟演示脚本

1. 运行 `python main_gui.py --demo`，直接进入离线示例，不依赖 API。
2. 展示「学习路线」：说明学情分析如何驱动个性化路线。
3. 展示「任务清单」：添加并勾选今日任务，说明执行闭环。
4. 打开「生成测验」，选择难度和数量，生成结构化题目。
5. 作答并点击「提交测验」，展示自动判分和逐题解析。
6. 展示「Agent 执行轨迹」，指出编排、工具调用和结果交付过程。
7. 回到「AI 辅导」，连续追问两轮，展示多轮上下文记忆。

## 技术亮点

- 原生 PySide6 桌面应用，无浏览器依赖，可直接打包为 Windows EXE。
- RAG 完全本地嵌入，不依赖 OpenAI 向量接口，适合国内网络。
- 搜索使用 Bing 中国区和 Baidu，失败时保留错误轨迹。
- 业务层与 GUI 分离，Agent 调用全部在后台线程，不阻塞界面。
- 通过 `schema.py`、`quiz_engine.py` 和 `prompts.yaml` 保证结构化输出可测试。

## 可复现方式

```bash
python -m venv .venv
.venv\Scripts\python.exe -m pip install -r requirements.txt
.venv\Scripts\python.exe main_gui.py --demo
python -m unittest discover -s tests -v
```

模型目录为 `assets/models/bge-small-zh-v1.5`；缺失时会自动从国内 Hugging Face 镜像下载。
