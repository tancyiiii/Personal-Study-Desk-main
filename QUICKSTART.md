# 快速入门指南

5 分钟内完成首次使用：配置 API、生成学习计划，然后开始学习。

## 1. 获取 API 密钥

程序通过 AI 提供商的 API 生成内容，首次使用前需要准备一个密钥。

推荐方案：

- **DeepSeek**：性价比高，适合日常学习。
- **Groq**：免费且响应快，适合快速测试。
- **OpenAI**：能力全面，需要付费 API 密钥。

获取密钥后，在应用左侧点击「设置」，选择对应提供商，粘贴 API 密钥并选择模型。

## 2. 启动应用

### 源码运行

```bash
pip install -r requirements.txt
pythonw main_gui.py
```

需要查看终端日志时：

```bash
python main_gui.py --console
```

### 打包版运行

直接双击：

```text
dist\StudyAssistant.exe
```

重新打包：

```bat
build_exe.bat
```

## 3. 生成学习计划

1. 选择学科分类，例如「编程」「数学」「语言」。
2. 填写主题、当前知识水平、学习目标、可用时间和学习风格。
3. 点击创建学习计划，等待 AI 完成分析、路线图和资源查找。
4. 完成后自动进入仪表盘，可通过顶部标签页切换内容。

## 4. 使用仪表盘

### 学习路线

- 查看分阶段学习路径和里程碑。
- 可重新生成路线图。
- 可下载为 Markdown 文件。

### 学习资源

- 查看 AI 推荐的课程、视频、书籍和练习平台。
- 可重新查找资源。
- 可下载资源清单。

### 生成测验

- 选择难度、题目数量和重点领域。
- 每次生成 5-20 道题。
- 可下载测验。

### AI 辅导

- 在底部输入问题，点击「发送」。
- 支持补充背景信息。
- 问答以聊天气泡显示，可连续追问。

### 文档问答（RAG）

- 点击「选择文件上传」，上传 PDF 或文本文件。
- 输入与文档内容相关的问题。
- 回答会基于上传的资料生成。

## 5. 历史生成记录

每次完整生成学习计划后，左侧「历史生成」列表会自动新增一条记录，显示主题和保存时间。

- 点击任意记录即可切换回那次的计划内容。
- 历史记录与当前计划保存在：

```text
%APPDATA%\StudyAssistant\study_assistant_state.json
```

## 6. 深色模式

1. 点击左侧「设置」。
2. 勾选「深色模式」。
3. 界面会立即切换，包括窗口标题栏、标签页、聊天气泡和文档回答区域。
4. 偏好会自动保存到：

```text
%APPDATA%\StudyAssistant\settings.json
```

## 学习计划示例

```text
学科：编程
主题：Python 数据分析
水平：初级
目标：能独立完成数据分析项目
时间：每周 5-8 小时
风格：视觉型
```

生成后建议：

1. 按路线图第一周内容学习。
2. 将教材上传到文档问答，按章节提问。
3. 每完成一个阶段做一次测验。
4. 遇到不懂的概念，直接在 AI 辅导中追问。

## 常见问题

### 生成失败

- 检查设置中的 API 密钥是否正确。
- 确认网络连接正常。
- 尝试切换到响应更快的模型。

### 窗口顶部栏没有跟随深色模式

- 旧版本需要重新打包或重启应用。
- 如果任务栏或标题栏仍显示旧样式，重启资源管理器后重试。

### 找不到历史记录

- 只有完整生成过计划才会写入历史。
- 若更换了系统用户目录，旧数据不会自动迁移。

### 文档上传后没有回答

- 确认文件格式为 PDF 或 TXT。
- 确认问题与文档内容相关。
- 尝试减少文件体积或重新提问。

## 更多文档

- [README.md](README.md)：项目总览。
- [ARCHITECTURE.md](ARCHITECTURE.md)：系统架构。
- [FILE_GUIDE.md](FILE_GUIDE.md)：文件说明。
