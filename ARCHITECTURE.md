 # 架构概览
 
 ## 多智能体系统设计
 
 本项目实现了一种**多智能体架构**，由专门的 AI 智能体协作提供全面的学习辅助。每个智能体都有特定的角色和专业领域。
 
 ## 系统架构
 
 ```
 ┌─────────────────────────────────────────────────────────────────┐
 │                    PySide6 Desktop GUI                          │
 │                       (main_gui.py)                               │
 └──────────────┬──────────────────────────────────────────────────┘
                │
                ▼
 ┌─────────────────────────────────────────────────────────────────┐
 │                 Agent Handler                                   │
 │             (business.py)                                │
 │ - Orchestrates agent workflows                                  │
 │ - Manages state and context                                     │
 │ - Coordinates multi-agent collaboration                         │
 └──────────────┬──────────────────────────────────────────────────┘
                │
      ┌─────────┴─────────┐──────────┐──────────┐
      ▼                   ▼          ▼          ▼
 ┌──────────┐ ┌──────────────┐ ┌──────────┐ ┌──────────────┐
 │ Student  │ │   Roadmap    │ │   Quiz   │ │    Tutor     │
 │ Analyzer │ │   Creator    │ │Generator │ │    Agent     │
 │  Agent   │ │    Agent     │ │  Agent   │ │              │
 └──────────┘ └──────────────┘ └──────────┘ └──────────────┘
      │              │              │              │
      └──────────────┴──────────────┴──────────────┘
                      │
                      ▼
         ┌─────────────────────────────┐
         │     Study Agents            │
         │   (study_agents.py)         │
         │ - Agent definitions         │
         │ - Persona management        │
         │ - Model configuration       │
         └─────────────────────────────┘
                      │
         ┌────────────┴────────────┐────────────┐
         ▼                        ▼            ▼
 ┌──────────────┐ ┌──────────────┐ ┌──────────────────┐
 │   Config     │ │   Prompts    │ │   RAG Helper     │
 │   Manager    │ │    YAML      │ │                  │
 │  (config.py) │ │              │ │ (rag_helper.py)  │
 └──────────────┘ └──────────────┘ └──────────────────┘
                                       │
                                       ▼
                               ┌─────────────────┐
                               │    ChromaDB      │
                               │  Vector Store    │
                               └─────────────────┘
 ```
 
 ## 智能体角色与职责
 
 ### 1. 学生分析智能体（Student Analyzer Agent）
 **目的**：评估学习需求并创建学生档案
 
 **能力**：
 - 分析当前知识水平
 - 识别学习差距
 - 确定先决条件
 - 推荐学习方法
 - 评估时间限制
 
 **输入**：学生信息（主题、水平、目标、时间、学习风格）
 **输出**：综合的学生分析报告
 
 **温度参数**：0.6（在创造力和一致性之间取得平衡）
 
 ---
 
 ### 2. 路线图创建智能体（Roadmap Creator Agent）
 **目的**：设计个性化学习路径
 
 **能力**：
 - 创建结构化的学习阶段
 - 按逻辑顺序排列主题
 - 估算时间需求
 - 设置里程碑和检查点
 - 适应学习风格
 
 **输入**：学生分析结果、目标、限制条件
 **输出**：包含各阶段的详细学习路线图
 
 **温度参数**：0.7（创造性规划）
 
 ---
 
 ### 3. 测验生成智能体（Quiz Generator Agent）
 **目的**：创建自适应评估
 
 **能力**：
 - 生成多种题型
 - 适应难度级别
 - 提供详细解释
 - 测试概念性和实践性知识
 - 聚焦特定领域
 
 **输入**：主题、难度、重点领域、题目数量
 **输出**：包含答案和解释的完整测验
 
 **温度参数**：0.5（一致的出题质量）
 
 ---
 
 ### 4. 辅导智能体（Tutor Agent）
 **目的**：解释概念并回答问题
 
 **能力**：
 - 分解复杂概念
 - 使用类比和示例
 - 适应学习风格
 - 提供逐步指导
 - 鼓励批判性思维
 
 **输入**：学生问题、上下文、知识水平
 **输出**：清晰的个性化解释
 
 **温度参数**：0.7（生动的解释）
 
 ---
 
 ### 5. 资源查找智能体（Resource Finder Agent）
 **目的**：搜索并推荐学习材料
 
 **能力**：
 - 搜索网络资源
 - 评估质量和相关性
 - 匹配学习风格
 - 寻找多样化的资源类型
 - 优先推荐免费资源
 
 **工具**：DuckDuckGo 搜索
 **输入**：主题、目标、水平、学习风格
 **输出**：精选的资源列表
 
 **温度参数**：0.6（平衡的搜索）
 
 ---
 
 ### 6. RAG 辅导智能体（RAG Tutor Agent）
 **目的**：使用上传的文档回答问题
 
 **能力**：
 - 检索相关文档上下文
 - 基于源材料提供答案
 - 引用特定章节
 - 识别信息缺口
 - 提供准确、上下文相关的答案
 
 **工具**：向量数据库（ChromaDB）
 **输入**：问题、文档上下文
 **输出**：带有来源引用的答案
 
 **温度参数**：0.6（准确的检索）
 
 ## 工作流模式
 
 ### 模式 1：初始学习计划创建
 ```
 用户输入 → 学生分析器 → 路线图创建器 → 资源查找器 → 仪表盘
 ```
 
 **步骤**：
 1. 用户提供学习目标和约束条件
 2. 学生分析器评估需求和差距
 3. 路线图创建器利用分析结果设计学习路径
 4. 资源查找器搜索学习材料
 5. 结果在仪表盘中展示
 
 **耗时**：约 30-60 秒
 
 ---
 
 ### 模式 2：测验生成
 ```
 用户请求 → 测验生成器 → 展示测验
 ```
 
 **步骤**：
 1. 用户指定难度、重点和题目数量
 2. 测验生成器创建带解释的题目
 3. 展示测验并提供下载选项
 
 **耗时**：约 10-20 秒
 
 ---
 
 ### 模式 3：辅导会话
 ```
 用户提问 → 辅导智能体 → 解释
 ```
 
 **步骤**：
 1. 用户提问（可附带上下文）
 2. 辅导智能体制定个性化解释
 3. 展示带有示例的回复
 
 **耗时**：约 5-15 秒
 
 ---
 
 ### 模式 4：RAG 文档问答
 ```
 用户上传 → 文档处理 → 向量存储
 用户提问 → 相似性搜索 → 上下文检索 → RAG 辅导 → 答案
 ```
 
 **步骤**：
 1. 用户上传 PDF/文本文档
 2. 文档被分割成块并嵌入向量
 3. 文本块存储在 ChromaDB 向量数据库中
 4. 用户提问
 5. 相似性搜索找到相关文本块
 6. RAG 辅导智能体利用上下文生成答案
 7. 展示带有引用的答案
 
 **耗时**：上传约 2-5 秒/文档，查询约 10-20 秒
 
 ## 组件详情
 
 ### Business Layer (`business.py`)
 **角色**：编排层
 
 **职责**：
 - 使用正确配置初始化智能体
 - 管理智能体执行顺序
 - 处理智能体调用之间的状态
 - 格式化包含上下文的提示词
 - 整合 RAG 功能
 - 通过 Qt 信号驱动 UI 更新
 
 **关键方法**：
 - `analyze_student()`：运行学生分析工作流
 - `create_roadmap()`：运行路线图创建工作流
 - `generate_quiz()`：运行测验生成工作流
 - `get_tutoring()`：单次辅导交互
 - `query_documents()`：基于 RAG 的文档问答
 
 ---
 
 ### Study Agents (`study_agents.py`)
 **角色**：智能体工厂
 
 **职责**：
 - 定义智能体配置
 - 从 YAML 加载角色设定
 - 配置模型（OpenAI/Groq）
 - 设置合适的温度参数
 - 管理学习风格适配
 
 **关键方法**：
 - `student_analyzer_agent()`：创建分析器
 - `roadmap_creator_agent()`：创建路线图设计师
 - `quiz_generator_agent()`：创建测验制作器
 - `tutor_agent()`：创建辅导
 - `resource_finder_agent()`：创建资源搜索器
 - `rag_tutor_agent()`：创建支持 RAG 的辅导
 
 ---
 
 ### RAG Helper (`rag_helper.py`)
 **角色**：文档处理和检索
 
 **职责**：
 - 加载和处理文档
 - 将文本分割成块
 - 创建嵌入向量
 - 管理向量数据库
 - 执行相似性搜索
 
 **关键方法**：
 - `load_pdf()`：处理 PDF 文件
 - `load_text()`：处理文本文件
 - `query()`：检索相关文本块
 - `clear_database()`：重置向量存储
 
 **技术栈**：
 - **LangChain**：文档处理
 - **ChromaDB**：向量存储
 - **OpenAI Embeddings**：文本嵌入
 - **RecursiveCharacterTextSplitter**：文本分块
 
 ---
 
 ### Config Manager (`config.py`)
 **角色**：配置访问
 
 **职责**：
 - 加载 YAML 配置
 - 提供角色定义
 - 管理提示词模板
 - 提供学习风格信息
 - 使用变量格式化提示词
 
 **单例模式**：确保唯一的配置实例
 
 ---
 
 ### Prompts YAML (`prompts.yaml`)
 **角色**：配置和提示词
 
 **内容**：
 - **角色（Personas）**：智能体系统提示词和能力描述
 - **提示词（Prompts）**：特定任务的提示词模板
 - **学习风格**：风格描述和建议
 - **学科分类**：主题列表和时长
 - **知识水平**：级别定义和方法
 
 ## 技术栈
 
 ### 核心框架
 - **Phidata**：多智能体编排
 - **PySide6 (Qt6)**：原生桌面界面
 - **Python 3.10+**：运行环境
 
 ### AI/ML
 - **OpenAI GPT-4**：高质量 LLM（付费）
 - **Groq Llama**：快速、免费的 LLM
 - **LangChain**：文档处理
 - **ChromaDB**：向量数据库
 - **OpenAI Embeddings**：文本嵌入
 
 ### 工具
 - **DuckDuckGo**：网络搜索
 - **PyPDF**：PDF 处理
 - **YAML**：配置
 - **python-dotenv**：环境管理
 
 ## 设计原则
 
 ### 1. 关注点分离
 每个组件都有单一、明确的职责：
 - 智能体：AI 能力
 - Handler：编排
 - RAG Helper：文档处理
 - Config：配置管理
 - App：用户界面
 
 ### 2. 模块化
 组件是松散耦合的，可以：
 - 独立修改
 - 孤立测试
 - 在不同场景中复用
 - 通过新功能扩展
 
 ### 3. 可配置性
 行为通过以下方式控制：
 - YAML 配置文件
 - 环境变量
 - 运行时参数
 - 用户偏好
 
 ### 4. 可扩展性
 架构支持：
 - 添加新的智能体
 - 新的学习风格
 - 更多的学科分类
 - 更多的文档类型
 - 不同的 LLM 提供商
 
 ### 5. 以用户为中心
 设计优先考虑：
 - 清晰的用户工作流
 - 即时反馈
 - 进度可视化
 - 错误处理
 - 有用的指导
 
 ## 性能考量
 
 ### 响应时间
 - **学生分析**：10-15 秒
 - **创建路线图**：15-20 秒
 - **查找资源**：10-15 秒（含网络搜索）
 - **生成测验**：10-15 秒
 - **辅导解答**：5-10 秒
 - **RAG 查询**：10-15 秒
 
 ### 优化策略
 1. **模型选择**：Groq 用于速度，GPT-4 用于质量
 2. **温度调优**：较低温度保证一致性，较高温度增强创造性
 3. **提示词工程**：清晰、具体的指令
 4. **缓存**：使用 WorkerThread 异步执行任务
 5. **分块**：RAG 的最佳分块大小（1000 字符）
 
 ## 安全考量
 
 ### API 密钥
 - 存储在 `.env` 文件中（不在代码中）
 - 绝不提交到版本控制
 - 通过 python-dotenv 加载
 
 ### 用户数据
 - 基于会话（不持久化）
 - 向量数据库仅对当前用户可用
 - 不传输外部数据（API 调用除外）
 
 ### 文件上传
 - 仅临时存储
 - 处理完毕后清理
 - 建议设置大小限制
 
 ## 扩展点
 
 ### 易于添加
 1. **新智能体**：添加到 `study_agents.py`
 2. **新提示词**：添加到 `prompts.yaml`
 3. **新学习风格**：添加到 YAML 配置
 4. **新学科分类**：添加到 YAML 配置
 5. **新的 LLM 提供商**：添加到 `_get_model()`
 
 ### 中等难度
 1. **进度追踪**：添加数据库层
 2. **用户账户**：添加身份验证
 3. **协作功能**：添加实时同步
 4. **移动应用**：创建 React Native 版本
 5. **分析仪表盘**：添加指标收集
 
 ### 复杂功能
 1. **间隔重复**：实现调度算法
 2. **语音交互**：添加语音识别/合成
 3. **实时辅导**：添加实时视频/聊天
 4. **平台集成**：连接 Coursera、Udemy 等
 5. **自适应学习**：基于 ML 的难度调整
 
 ## 延伸阅读
 
 - [Phidata 文档](https://docs.phidata.com/)
 - [LangChain RAG 教程](https://python.langchain.com/docs/use_cases/question_answering/)
 - [PySide6 文档](https://doc.qt.io/qtforpython/)
 - [多智能体系统](https://en.wikipedia.org/wiki/Multi-agent_system)
 
 ---
 
 **这套架构提供了一个强大、灵活且可扩展的学习平台，能够适应每位学生的独特需求！**
